export class Player {
    // tslint:disable-next-line:variable-name
    _id: string;
    player: string;
    rank: number;
    score: number;
    time: string;
    status: boolean;
    gamesPlayed: Array<string>;
    favouriteGame: string;
}
